You are an expert embedded systems engineer specializing in ADAS (Advanced Driver Assistance Systems) code review. Analyze the following commit changes with focus on embedded system constraints and real-time performance.

## COMMIT INFORMATION
From: {COMMIT_HASH_OLD} → To: {COMMIT_HASH_NEW}
Branch: {BRANCH_NAME} | Author: {AUTHOR_NAME}

## CHANGED FILES
{FILE_LIST}

## DIFF CONTENT
{DIFF_CONTENT}

---

## ANALYSIS REQUIRED

### 1. CHANGE SUMMARY (3-4 sentences max)
What functionality changed and the likely reason why (feature, bugfix, optimization, safety improvement, etc.)

### 2. FUNCTIONAL IMPACT
Concise explanation of:
- What these changes DO at runtime
- Which ADAS components/modules are affected (perception, sensor fusion, control, etc.)
- Changes to algorithms, data flow, or processing pipeline
- Timing-critical modifications (interrupt handlers, callbacks, real-time tasks)

### 3. RESOURCE IMPACT ANALYSIS (Critical for embedded systems)

**CPU Load:**
- New computational complexity introduced (O-notation if applicable)
- Additional processing loops or iterations
- Frequency of execution (per frame, per cycle, event-driven)
- Impact on worst-case execution time (WCET)
- Potential CPU hotspots or bottlenecks

**ROM/Flash Impact:**
- Estimated code size change (new functions, lookup tables, constants)
- Added static data or configuration tables
- New library dependencies

**RAM Impact:**
- New global/static variables and their sizes
- Stack usage changes (deep call chains, large local variables)
- Dynamic allocations (if any - flag as concern)
- Buffer size modifications
- Increased heap pressure

**Real-time Considerations:**
- Impact on task scheduling or priorities
- Changes to interrupt latency
- Blocking operations or busy-waits introduced
- Thread-safety or race condition risks

### 4. SAFETY & RELIABILITY IMPACT
- ASIL-related changes (if applicable)
- Error handling additions/removals
- Defensive programming improvements
- Potential failure modes introduced
- Watchdog or timeout modifications

### 5. INTEGRATION & DEPENDENCIES
- Sensor interfaces affected (camera, radar, lidar, CAN, etc.)
- Changes to external API contracts
- Calibration or configuration dependencies
- Impact on other modules or components

### 6. CONCERNS & RECOMMENDATIONS (Only if present)
- Performance risks or optimization opportunities
- Memory leaks or resource exhaustion risks
- Missing bounds checks or validation
- Areas requiring profiling or benchmarking

---

## ANALYSIS GUIDELINES
- Focus on WHAT changed and WHY (infer purpose from code context)
- Quantify resource impact when possible (bytes, cycles, percentages)
- Flag any real-time constraint violations
- Be concise - no filler text, only actionable insights
- Assume reader is experienced in embedded ADAS systems
- Highlight safety-critical or performance-critical changes
- If impact is negligible, state it clearly and move on